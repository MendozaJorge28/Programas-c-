#include <iostream>
using namespace std;

int main() {
    float a, e, l;
    cout << "Ingrese la base del rectangulo: ";
    cin >> a;
    cout << "Ingrese la altura del rectangulo: ";
    cin >> e;
    l = a * e;
    cout << "El area del rectangulo es de: " << l << endl;

    return 0;
}
